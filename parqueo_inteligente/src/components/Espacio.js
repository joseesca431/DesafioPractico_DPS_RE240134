import React from "react";
function estacionamiento({id,numero, disponible, tiempoReserva}){
    const estadoClase = disponible ? "btn-success" : "btn-danger";
    const estadoTexto = disponible ? "Disponible" : "Reservada";

    return(
        <>
        <button>
            
        </button>
        </>
    )
}